{
  "name": "@defra/wls-api",
  "description": "The API to support the wildlife licencing of European Protected Species",
  "main": "src/api-service.js",
  "type": "module",
  "private": true,
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": ">=16.13.0"
  },
  "scripts": {
    "start": "npm install -g sequelize-cli; npm run database:migration:migrate; node src/api-service.js;",
    "dev": "nodemon -r dotenv/config src/api-service.js",
    "test": "echo \"Error: run tests from root\" && exit 1",
    "postinstall": "if [ \"${NODE_ENV:-development}\" == \"development\" ]; then npx webpack-cli; else echo no webpack build; fi;",
    "database:migration:create": "npx sequelize-cli migration:generate --name migration; echo \"\nPlease change generated file extension from .js to .cjs before running migrate script.\"; echo \"\nEnsure that openapi/licence.yaml has also been updated with fields that correspond to any new columns.\";",
    "database:migration:migrate": "npx sequelize-cli db:migrate --url \"postgres://${POSTGRES_USER}:${POSTGRES_PW}@${POSTGRES_HOST}/${POSTGRES_DB}\"",
    "database:migration:migrate:undo": "npx sequelize-cli db:migrate:undo --url \"postgres://${POSTGRES_USER}:${POSTGRES_PW}@${POSTGRES_HOST}/${POSTGRES_DB}\""
  },
  "keywords": [
    "API",
    "Wildlife",
    "Licencing"
  ],
  "author": "Graham Willis",
  "license": "SEE LICENSE IN LICENCE",
  "repository": {
    "type": "git",
    "url": "https://github.com/DEFRA/wildlife-licencing.git"
  },
  "dependencies": {
    "@hapi/hapi": "^20.2.1",
    "@hapi/inert": "^6.0.4",
    "@hapi/vision": "^6.1.0",
    "argon2": "^0.30.1",
    "dotenv": "^10.0.0",
    "openapi-backend": "^5.0.0",
    "sequelize": "^6.12.5",
    "swagger-ui": "^4.1.0",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "css-loader": "^6.5.1",
    "html-webpack-plugin": "^5.5.0",
    "json-loader": "^0.5.7",
    "style-loader": "^3.3.1",
    "webpack": "^5.64.0",
    "webpack-cli": "^4.9.1",
    "yaml-loader": "^0.6.0"
  },
  "gitHead": "8b51186f90ae03e7c8a4550e1ab21c868f8e6af3"
}
