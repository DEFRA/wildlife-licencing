{% extends "postcode.njk" %}

{% if data.accountName %}
    {% set title = 'What is ' + data.contactName + '\'s postcode at ' + data.accountName + '?' %}
{% else %}
    {% set title = 'What is ' + data.contactName + '\'s postcode?' %}
{% endif %}
