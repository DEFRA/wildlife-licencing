{% extends "email-address.njk" %}

{% set hintText = 'We’ll only use this to contact you about the application.' %}

{% if data.accountName %}
    {% set title = 'What is ' + data.contactName + '\'s email address at ' + data.accountName + '?' %}
{% else %}
    {% set title = 'What is ' + data.contactName + '\'s email address?' %}
{% endif %}
