{% extends "check-answers.njk" %}

{% set questionMap = {
    contactIsUser: { question: 'Is the licence for you?', answer: { 'yes': 'Yes', 'no': 'No', '-': 'Not answered' }, href: _uri.applicantUser },
    whoIsTheLicenceFor: { question: 'Who is the licence for?', href: _uri.applicantName },
    contactIsOrganisation: { question: 'Are you part of an organisation?', answer: { 'yes': 'Yes', 'no': 'No', '-': 'Not answered' }, href: _uri.applicantIsOrganisation if data.hasAccount else _uri.applicantOrganisations },
    applicantOrganisations: { question: 'Name of organisation', href: _uri.applicantOrganisations },
    email: { question: 'Organisation email' if data.hasAccount else 'Contact email', href: _uri.applicantEmail if not data.immutableAccount },
    address: { question: 'Organisation address' if data.hasAccount else 'Contact address',  href: _uri.applicantEmail if not data.immutableAccount }
} %}

{% set checkData = data.checkYourAnswers %}
