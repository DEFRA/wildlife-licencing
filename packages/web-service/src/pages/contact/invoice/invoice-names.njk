{% extends "contact-names.njk" %}
{% set title = 'Who is responsible for paying the invoice?' %}
{% set addText = 'Add someone else' %}

{% set errorMap = {
        'contact': {
            'any.required': { ref: '#contact', text: "Choose the payer or 'someone else'" }
        }
    }
%}
