// import { APPLICATIONS, TASKLIST, contactURIs } from '../../../../uris.js'
// const { APPLICANT: { NAME } } = contactURIs

describe('ecologist-names', () => {
  beforeEach(() => jest.resetModules())
  it('correctly calls the underlying contact function', async () => {
  })
})
