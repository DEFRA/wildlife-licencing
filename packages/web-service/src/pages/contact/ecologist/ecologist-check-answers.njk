{% extends "check-answers.njk" %}

{% set questionMap = {
    whoIsTheLicenceFor: { question: 'Who is the ecologist?', href: _uri.ecologistNames },
    contactIsOrganisation: { question: 'Is the ecologist part of an organisation?', answer: { 'yes': 'Yes', 'no': 'No', '-': 'Not answered' }, href: _uri.ecologistIsOrganisation if data.hasAccount else _uri.ecologistOrganisations },
    contactOrganisations: { question: 'Name of organisation', href: _uri.ecologistOrganisations },
    email: { question: 'Organisation email' if data.hasAccount else 'Contact email', href: _uri.ecologistEmail },
    address: { question: 'Organisation address' if data.hasAccount else 'Contact address',  href: _uri.ecologistPostcode }
} %}

{% set checkData = data.checkYourAnswers %}
