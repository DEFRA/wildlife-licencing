{% extends "contact-names.njk" %}
{% set title = 'Who is the ecologist?' %}
{% set addText = 'Add a new ecologist' %}

{% set errorMap = {
        'contact': {
            'any.required': { ref: '#contact', text: "Choose the ecologist or 'someone else'" }
        }
    }
%}
