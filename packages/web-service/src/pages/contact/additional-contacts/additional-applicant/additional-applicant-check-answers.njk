{% extends "information-page.njk" %}
{% from "button/macro.njk" import govukButton %}
{% set widthClass = 'govuk-grid-column-two-thirds' %}
{% set title = 'Check your answers' %}
{% from "answer-summary.njk" import answerSummary %}

{% set questionMap = {
    addAdditionalApplicant: { question: 'Do you want to add an alternative licence holder contact?', answer: { 'yes': 'Yes', 'no': 'No', '-': 'Not answered' }, href: _uri.additionalApplicantAdd },
    applicantName: { question: 'What is the name of the person we should contact?', href: _uri.additionalApplicantName },
    applicantEmail: { question: 'What is the email address of the person we should contact?', href: _uri.additionalApplicantEmail }
} %}

{% block pageContent %}
    {{ answerSummary(questionMap, data.applicant ) }}

    {{ govukButton({
        attributes: { id: 'continue' },
        preventDoubleClick: true,
        name: 'continue',
        text: buttonText if buttonText else 'Continue',
        classes: "govuk-!-margin-top-5"
    }) }}
{% endblock %}
