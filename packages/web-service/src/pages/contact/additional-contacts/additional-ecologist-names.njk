{% extends "contact-names.njk" %}
{% set title = 'Who is the person we should contact?' %}
{% set addText = 'Someone else' %}

{% set errorMap = {
        'contact': {
            'any.required': { ref: '#contact', text: "Choose the person we should contact or 'someone else'" }
        }
    }
%}
