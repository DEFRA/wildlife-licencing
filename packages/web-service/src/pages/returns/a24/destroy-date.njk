{% extends "standard-form.njk" %}

{% from "govuk/components/date-input/macro.njk" import govukDateInput %}

{% set errorMap = {
        'destruction-date': {
          'noDateSent': { ref: '#destruction-date', text: 'You have not entered the date you destroyed the vacant sett' },
          'invalidDate': { ref: '#destruction-date', text: 'The date must be a real date' },
          'dataInFuture': { ref: '#destruction-date', text: 'The date must be in the past' }
        }
    }
%}

{% set title = 'When did you destroy the vacant sett?' %}

{% block pageContent %}
  {{ govukDateInput({
    id: "destruction-date",
    namePrefix: "destroy-date",
    name: "destruction-date",
    errorMessage: { text: "Enter the date you destroyed the vacant sett" } if error['destruction-date'],
    items: [
      {
        classes: "govuk-input--width-2",
        value: data.day,
        name: "day"
      },
      {
        classes: "govuk-input--width-2",
        value: data.month,
        name: "month"
      },
      {
        classes: "govuk-input--width-4",
        value: data.year,
        name: "year"
      }
    ],
    hint: {
      text: "For example, 27 3 2007"
    }
  }) }}

{% endblock %}
