{% extends "standard-form.njk" %}

{% from "govuk/components/input/macro.njk" import govukInput %}

{% set errorMap = {
        'habitat-grid-ref': {
          'no-reference-sent': { ref: '#habitat-grid-ref', text: 'You have not entered a grid reference.' }
        }
    }
%}

{% set title = "What is the badger sett's grid reference?" %}

{% block pageContent %}

  {{ govukInput({
    id: "habitat-grid-ref",
    name: "habitat-grid-ref",
    hint: {
      text: "For example, NY395557. Use the grid reference for the central point of the sett if it has more than one entrance."
    },
    errorMessage: { text: "Enter a national grid reference for the sett." } if error['habitat-grid-ref'],
    classes: "govuk-input--width-10",
    spellcheck: false
  }) }}

{% endblock %}