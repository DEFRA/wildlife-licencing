{% extends "standard-form.njk" %}

{% from "error-summary.njk" import errorSummary %}
{% from "govuk/components/radios/macro.njk" import govukRadios %}
{% from "govuk/components/button/macro.njk" import govukButton %}

{% set errorMap = {
        'habitat-reopen': {
          'no-radio-selected': { ref: '#sett-radio', text: 'You have not selected a choice.' }
        }
    }
%}

{% block pageTitle %}{{ 'Error: ' + title if error else title }}{% endblock %}

{% block pageContent %}

  {{ errorSummary(error, errorMap, 'There is a problem') }}

  {{ govukRadios({
    id: "settReopen-radio",
    name: "settReopen-radio",
    fieldset: {
      legend: {
        text: "Will the badger sett be reopened once development is complete?",
        classes: "govuk-fieldset__legend--l"
      }
    },
    errorMessage: { text: "Select yes if the badger sett will be reopened once development is complete." } if error['sett-active'],
    items: [
      {
        value: true,
        text: "Yes"
      },
      {
        value: false,
        text: "No"
      }
    ]
  }) }}

{% endblock %}