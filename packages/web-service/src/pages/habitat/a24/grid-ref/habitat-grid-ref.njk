{% extends "standard-form.njk" %}

{% from "govuk/components/input/macro.njk" import govukInput %}

{% set errorMap = {
        'habitat-grid-ref': {
          'string.empty': { ref: '#habitat-grid-ref', text: 'Enter a grid reference' },
          'string.pattern.base': { ref: '#habitat-grid-ref', text: 'A grid reference must be 2 letters, followed by 6 numbers' }
        }
    }
%}

{% set title = "What is the badger sett's grid reference?" %}

{% block pageContent %}

  {{ govukInput({
    id: "habitat-grid-ref",
    name: "habitat-grid-ref",
    hint: {
      text: "For example, NY395557. Use the grid reference for the central point of the sett if it has more than one entrance."
    },
    errorMessage: { text: "Enter a national grid reference that is 2 letters and 6 digits. " } if error['habitat-grid-ref'],
    classes: "govuk-input--width-10",
    value: data.gridReference,
    spellcheck: false
  }) }}

{% endblock %}
