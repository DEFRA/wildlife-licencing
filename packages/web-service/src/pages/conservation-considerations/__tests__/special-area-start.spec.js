jest.spyOn(console, 'error').mockImplementation(() => null)

describe('the special area start functions', () => {
  beforeEach(() => jest.resetModules())

  describe('the getData function', () => {
    it('smokes', async () => {
      /// const { getData } = await import('../special-area-start.js')
    })
  })
})
